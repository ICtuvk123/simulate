# 第二问重定义：覆盖率与剩余总时间

2026-09-11 新增用户指令：按重新定义的第二问设计算法，使用 Goal，禁止正式测试。

本阶段应用赛题分析建模、编程与图表两个技能。原有 Goal 的工具状态为 paused；当前工具不能恢复或替换尚未完成的 Goal，本轮仍执行明确授权的算法工作。

## 工作范围

1. 不考虑半径 1800 米目标圆的裁剪；将首站平移旋转为原点、示向度为 0。
2. 纠正鲁棒安全域，证明三个圆交集及角度端点化的适用条件。
3. 保留几何覆盖率主目标；另给覆盖率容差内的时间选择与时间优先的 Pareto 方案。
4. 将逐一枚举真实位置和测向误差，改为对可能的第二次示向度进行区间细分；连续误差通过集合包含关系验证。
5. 实现离线候选搜索、同一组几何场景下的后续清除成本比较、独立验证、可复现数据与图表。
6. 仅做离线计算；本阶段不操作模拟器、不调用其 HTTP 接口、不读取隐藏坐标或内部文件。

## 输出结构

- code/：几何、安全域、观测区间、候选搜索与离线验证。
- tests/：安全域反例、区间包含、覆盖率界与安全清除位置检查。
- outputs/：候选点、覆盖率上下界、时间分解、验证配置与摘要。
- figures/：由输出数据生成的 PDF。
- reports/：建模设计、结果及限制。

## 判定纪律

覆盖率上下界针对公开几何模型；候选点仅在已搜索点集中优化，不声称二维连续全局最优。几何测试场景为自行构造的评估积分点，不能写成官方演练或官方案例种子。固定地点的误差保持不变。估计剩余时间与有证明的安全清除条件分开。
