# 冻结 F 策略：官方问题三单次演练结果

2026 年 9 月 12 日按用户“跑一遍官方的演练测试”的授权执行一次问题三演练。案例编码为 `ZHDW-2XXU-YVFQ-GKZB`。未启动正式测试，未使用正式测试次数。

**12/12 个全向干扰源全部清除，正常退出。平均定位清除时间为 233.96 秒/源，总行动耗时为 2807.54 秒。**

| 指标 | 结果 |
|---|---:|
| 官方演练结束后显示的源数量 | 12 |
| 接口确认清除数量 | 12 |
| 清除率 | 100% |
| 总行动耗时 | 2807.541047 秒 |
| 每源平均定位清除时间（总时间 ÷ 清除数） | 233.961754 秒/源 |
| 移动时间 | 2051.541048 秒 |
| 射频检测时间 | 585 秒（117 次） |
| 切换频道时间 | 111 秒（111 次） |
| 光学定位时间 | 36 秒（12 次） |
| 清除时间 | 24 秒（12 次） |
| 行走距离 | 10257.705239 米 |
| 程序实际运行时间（日志测得） | 3.36 秒 |
| 失败动作／被拒动作／网络重试 | 0／0／0 |

本案例的每源平均时间落在 200–300 秒区间。总行动耗时约为 46 分 47.54 秒；程序实际运行时间与题目计费的行动时间是不同指标。单次随机演练不能证明跨案例总体均值，也不能用不同案例的结果直接断言 F 优于其他策略。

使用版本 `structural-F-budgeted_through-20260912`，参数与先前本地验证的冻结 F 一致。连接前已验证冻结文件，并从官方当前界面核实“问题3 演练 测试”“等待机器狗进入”和实际案例编码。接口适配器仅访问官方本机的四个文档端点，接收正常测量与清除反馈。

终止前，算法已清除全部已发现频道，并由每个剩余频道的实际负反馈完成区域覆盖证明。官方源数量在算法退出后才读取，用于核对 12/12，未作为决策输入。

纯反馈复现检查通过：**131 条动作全部与本次官方执行完全一致**。复现时禁止决策过程访问磁盘、网络和外部进程，未导入自建模拟器引擎，未传入随机种子或真实源坐标。

同目录证据包括 `requests.jsonl`（全部请求与原始反馈）、`metrics.json`（行动计时与完成证明）、`ui_mode_proof.json`（连接前模式核验）、`post_run_ui_evaluation.json`（退出后官方数量核对）、`feedback_only_audit.json`（纯反馈复现结果）、`frozen_policy_verification.json` 与 `source/`（执行代码快照）。原始 metrics 中源数量保留为未知，退出后核验单独存储，避免混入决策输入。

官方界面已确认保存行为日志 `practice-p3-219038142041916254-ZHDW-2XXU-YVFQ-GKZB.jlog`（43.7 KiB）。该原始日志应保持官方文件名和内容不变。

该 `.jlog` 尚未另行导出到本目录：恢复窗口时界面工具持续返回 `window is minimized` 或 `user input was detected in this window; call get_window_state before continuing`，重新选择窗口并恢复后仍未成功，因此停止继续操作窗口。模拟器内的日志已保存；本目录完整接口记录、计时结果、代码快照和审计结果均已成功归档，此限制不影响演练完成或上述计时核验。
