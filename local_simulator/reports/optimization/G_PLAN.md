# G：继续压缩总行动时间的本地实验

2026-09-12，用户授权“继续尝试压缩总时间”。全部实验在自建模拟器运行，禁止官方演练及正式测试；保持冻结 F 控制器、参数和环境计时规则不变。

先独立比较两个结构改动：首次观测后选择覆盖六边形方向；在每次重规划中将前两个搜索站向实际任务路线移动，只有逐频道历史负反馈加剩余计划仍通过保守连续覆盖检查才接受。未来目标位置只参与规划，不参加结束证明。

训练第一轮 9001–9012：冻结 F、旋转、变形、旋转加变形。后续可在相同训练集和扩展训练集 9021–9040 排除失败方向、分析组件。未使用验证集选参数。

最终验证预留新案例 10001–10100，冻结候选后再一次性运行 F 与候选各 100 局，不根据验证中途结果调参。压力集 10201–10208 包括边界、聚集、最小接收半径、恒定极值及平滑误差。记录所有运行，包括失败和未采纳方案。

评价主指标为整局总行动耗时；同时报告每局时间除以清除数的均值、P90/P95、最坏时间、失败情况和真实反馈完成证书。采用标准为全部清除且通过计时和完成核验，独立验证平均总时间显著下降、每源时间下降、压力组全清。若最坏时间明显变差，单独披露并保留 F。所有新候选完成纯反馈重放审计，不将场景种子、真实源数和坐标输入决策。

训练续记：第二轮在 9021–9040 比较 F、粗粒度移动、连续线段搜索后通过保守覆盖检查的精细移动。第三轮回查 9001–9012，精细方案在此组退步，因此没有直接进入验证。第四轮在全部 32 个训练场景比较 F、带前后任务全可行域不增程约束的粗粒度移动及精细移动。对相邻目标的每个可行多边形顶点检查新旧搜索点距离平方之差；该差是目标位置的仿射函数，因此顶点检查可证明整个凸区域的局部不增程。不能据此声称后续信息变化下全局时间一定降低。

在尚未运行验证集前，明确最坏案例采纳门槛：G 验证组最坏总时间不得高于 F 最坏时间的 102%。超过则保持 F 默认，另行披露候选平均收益。候选代码与参数冻结后才开始 10001–10100，验证中途不改参数。
