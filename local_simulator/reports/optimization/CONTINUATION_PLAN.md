# 后续本地优化计划（2026-09-11）

用户要求继续优化自建模拟器的整局总时间。官方问题三演练已结束，不再连接官方程序；禁止正式测试。

保留原冻结 D 为对照，新增 E 实验策略，物理引擎、速度、误差界、动作费用、隐藏信息隔离与最终完成证明均不改变。所有运行逐动作保存日志与源代码快照。

第一轮：在已有训练场景 301–308 上比较：每次新测向后重新决定下一目标；按照首次观测选择六站圆环转角；用于路线评分的面积重心；按扇形面积加权的候选测站评价。假设位置仅从观测可行多边形生成，不访问真值。候选失败则保留 D。

第二轮：把胜出组件及相关参数组合在 501–512 上检查并细化。这组是扩展训练，不宣称独立最终验证。

最终冻结后才启用新的 6001–6030，D 与胜出 E 使用完全相同的场景进行配对验证。此前 2001–2020 的验证结果作为历史结果保留，不再称为本轮未见验证。压力场景另用 6101–6104。

启用验证前的计划修订：扩展训练的改善约为 1%，因此在任何新验证场景尚未运行时，将最终验证预留扩展到 **6001–6100，共 100 个配对场景**。全部运行后统一评价，不按中途结果提前结束或调参。只有全清、证书通过、平均总时间下降且配对 bootstrap 95% 改善区间下界大于 0，才把 E 设为推荐默认；否则保留 D 并如实报告候选结果。原定压力场景 6101–6104 不变。

优先级：所有场景全清并通过逐频道覆盖证明，其次最小整局动作总时间。报告移动、RF、切频、光学、清除分项，均值、中位数、P90/P95、最坏值与配对差异。总时间下降才采纳，不以单分项收益替代。
