# 问题三当前验证结果

最新状态（2026-09-11）：用户已改为仅使用自建模拟器。最新成果见 `../local_simulator/reports/RESULTS_REPORT.md`：19项规则测试及36次本地配对运行通过，界面与离线回放已完成。下文为此前部署阶段的历史报告，不代表当前实现进度。正式测试从未执行。

## 运行环境

Windows，捆绑Python：`C:\Users\12831\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`。控制和评估代码只使用标准库，无新增依赖。

## 已实现内容

- `code/q3client.py`：文档四端点请求验证、仅回环HTTP连接、串行调用、丢失响应原ID原内容重试、未解决动作阻止新动作、已接受动作账本、时间预算、持久化JSONL及离线解析。
- `code/experiment.py`：附件计时示例离线检查、真实演练待机后的最小连接演示、日志解析；不包含搜索策略和正式模式启动功能。
- `tests/test_contract.py`：协议和计时边界测试。

记录的指标包括run_id、seed（未知则null）、jammer_count（仅退出后评估补入）、clear_count、clear_ratio、total_time、所有动作时间、动作次数、轨迹、各频道事件、失败类型、原始客户端日志路径、策略SHA256和参数SHA256。当前没有代码提交可用，commit为null。

## 实际执行的验证

已执行 `python -m unittest discover -s tests -v`：21项全部通过，测试执行用时0.149秒。覆盖：

- 附件示例199秒；清除成功分支201秒。
- clear不改变RF频道，重复同ID响应不重复计时。
- accepted=false不改变位置、频道及既有虚拟时间。
- 浮点微秒计时、时钟不符可见。
- near无示向、direction必须含合法示向。
- 不合法位置/频道/字段/路径/标识符拒绝。
- 网络丢失响应重试保持完全相同字节，未知执行结果阻止新动作。
- enter返回的短预算生效。
- 原始日志解析前后内容一致，已有原始文件不能覆盖。
- 退出前禁止补入总数，正式模式不接受题面未公开的总数，未知清除比例为null。

另执行 `python code/experiment.py inspect-sample`，输出如下：

| 项目 | 数值 |
| --- | ---: |
| 移动距离 | 900米 |
| 移动时间 | 180秒 |
| RF检测次数 | 3 |
| RF时间 | 15秒 |
| 切频次数/时间 | 1次/1秒 |
| 光学次数/时间 | 1次/3秒 |
| 成功清除时间 | 0秒 |
| 合计 | 199秒 |

这张表来自附件公开计时例。RF结果在测试夹具中显式设为no_signal以驱动分支，属于人工构造的测试输入，不是任何真实场景测量。成功分支只是合同中成功操作比失败操作多2秒的校验。

## 与建模报告一致性

控制状态不接收源数量和位置；评估总数只允许在成功exit后由用户提供。日志解析不会发送网络请求。客户端日志与正式加密日志分开，当前没有创建或替换任何官方日志。

21项测试验证的是本地代码对附件的实现，不证明官方HTTP服务实际行为、真实动作链、搜索完备性或100%清除率。尚未完成问题1/2定位代码、问题3基线和自适应策略。

## 外部阻塞与剩余工作

模拟器未下载，网盘浏览器访问被站点安全策略阻止。需用户手动下载、解压并以参赛身份登录，提供程序路径及robot_id。随后才能核实真实连接、规则机制、完整演练、日志导出。

文档未提供seed、重开同案例、自动reset或隐藏坐标导出。不能伪造同seed配对实验或oracle。若无官方支持，相关需求应明确标记不可用，并与用户确定允许的替代评估设计。

训练日志、leaderboard、消融、oracle结果和性能图暂不生成虚构数据；三次正式测试均未执行。没有达到优化平台期，也没有宣布目标完成。
