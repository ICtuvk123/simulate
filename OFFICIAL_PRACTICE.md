# 官方演练操作说明

本轮研发只运行了自建本地模拟器。没有启动官方演练或正式测试，没有消耗正式测试次数。下面是给操作人员日后手动运行的说明；随包的官方连接适配器目前只通过了模拟连接单测，尚未经官方演练验证。

1. 先在本地运行 `python q4.py --sources 14 --seed 1 --replay-check`，确认环境和冻结版本能正常运行。
2. 自行打开官方模拟器并登录，在界面选择**问题4演练测试**。核对当前模式，不能选择正式测试。官方四个接口没有提供可供脚本核验的测试模式字段，因此这个检查必须在官方界面完成。
3. 在本文件所在目录打开终端。按官方界面开始演练，等倒计时结束、接口开放后运行：

   `python official_practice.py --robot-id 你的参赛队号 --confirm-practice I_HAVE_SELECTED_PRACTICE`

4. 如果手动修改过官方端口，追加 `--url http://127.0.0.1:新端口`。脚本只接受数字回环地址和四个公开动作接口，不查询隐藏场景或额外接口。
5. 程序从 `INCUMBENT.json` 读取冻结配置；逐次发送动作，并按 `/enter` 的实际剩余现实时间控制预算。光学清除不修改无线频道。网络响应不确定时，客户端沿用原请求编号和原请求体重试。
6. 成功运行后可对 `official_practice_logs/本次目录/requests.jsonl` 运行 `python code/verify_exit.py 日志路径`。它只检查实际反馈中的完成证书与计时，不需要官方源坐标。官方最终统计以模拟器界面为准。

如果显示连接未开放或会话已停止，先看模拟器界面的当前状态；不要把连接错误解释为无信号，也不要在状态不明时换新请求编号继续动作。这个入口不自动登录、不自动开始测试、不修改官方程序、不切换测试模式。

正式测试不在本包的运行流程和本次授权范围内。`q4.py` 始终只启动自建本地模拟器；官方演练入口是独立的 `official_practice.py`，不会被实验脚本调用。
