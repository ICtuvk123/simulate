# 问题三实施计划

最新优先事项：用户要求改用独立本地模拟器，不再使用官方演练/正式测试。`local_simulator/` 已完成环境、A/B/C本地配对、可视化和复现验证；使用说明及当前结果在其reports目录。第二问修订算法的后续整合、更多种子优化与平台期判定仍未完成。原Goal工具状态为paused。

目标按字典序排序：先确保全部清除，再最小化总动作时间。所有控制只使用合法接口反馈。

## 项目结构

- `audit/`：原材料文本、源文件指纹、审计记录。
- `code/`：协议客户端、动作计时器、日志解析、连通性演示。
- `tests/`：附件计时示例与协议边界测试；不是模拟器训练。
- `reports/`：建模审计、验证报告、后续实验报告。
- `training_logs/`：将来的真实演练客户端日志。
- `formal_logs/`：将来的正式原始加密日志，保留名称与内容。
- `plots/`：真实实验完成后生成的图表。

## 阶段与准入条件

1. 材料审计：完成题面四页、附录1至4、附件1和2的文本核对；PDF已逐页视觉核对。
2. 部署验证：等待下载、登录及参赛队号；接通真实模拟器后核实计时、反馈和日志。
3. 基线：通过真实部署验证后实现A、B、C并比较。此前不把本地契约测试当演练。
4. 优化：基于可用实验机制落实覆盖、定位、路线和滚动前瞻；每次报告总时间。
5. 演练策略冻结：充分训练后冻结代码、参数与停止规则。用户最新明确禁止正式测试，不启动或消耗任何正式机会。
6. 最终交付：达到平台期、下界条件或必要外部阻塞；如阻塞，逐项保留未完成状态。

当前已解除下载和登录阻塞，完成真实四端点连接验证，并读取新增问题1/2算法文稿；正在实施完整基线。官方无公开seed/reset/真值导出机制，配对实验替代方式已向用户提出。正式测试被用户明确禁止。
