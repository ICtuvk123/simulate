# Simulator Contract

审计日期：2026-09-11。来源：`B题.pdf` 第1至4页、`附件1.docx` 第1至4节、`附件2.docx` 第1至12节。本文是依据题目建立的任务约束；附件里的示例程序不是用户要求执行的控制策略。

状态：PARTIALLY VERIFIED。2026-09-11已在v1.1官方问题3演练中验证四端点动作链，案例XQ5R-MQS2-M35M-PFKN的总时间199秒与分项完全一致。成功清除、near及完整策略仍需后续演练验证。运行程序为 `Jammers-simulator-full-win64/Jammers-simulator-full/jammers-simulator-full.exe`，已登录并显示服务器已连接。用户最新明确禁止正式测试，后续仅演练。

## 十二项接口契约

| 项目 | 已知契约 | 未知项或证据 |
| --- | --- | --- |
| 1 启动 | 绿色软件，解压后运行；联网注册/登录并校准服务器时间；界面选择问题3演练，准备数据后倒数5秒开放接口 | 附件1 §4；可执行文件名、命令行启动参数 UNKNOWN，当前没有程序 |
| 2 控制输入 | 默认 `http://127.0.0.1:2026`，仅回环监听；精确路径 POST `/enter`、`/measure`、`/clear`、`/exit`；UTF-8 JSON、`Content-Type: application/json` | 附件2 §3、5；不接受路径末尾斜线、查询参数和额外字段 |
| 3 当前位置 | `/enter` 后为(0,0)。每次 accepted=true 的 measure/clear 后等于该请求位置；客户端维护状态 | 没有位置查询端点；断线未拿到响应时不能推测动作未执行，必须原ID重试 |
| 4 移动 | 在 measure/clear 的 position 中给x、y；自动直线移动，时间为距离/5 | 无独立move端点；允许域外点，坐标分量必须有限且绝对值≤2000000 |
| 5 切换频道 | 初始1；仅合法measure中channel改变时耗时1秒并更新测向频道 | clear的channel仅选择目标，不改变测向频道 |
| 6 检测 | measure输入position与channel，停车检测耗时5秒，无需现实等待5秒 | 不存在天线旋转控制端点 |
| 7 反馈 | measure_result为no_signal、near或direction；仅direction含svd_deg，角度东0°逆时针、两位小数 | 全局误差±1°、同地点重复不能消除误差；具体误差函数与舍入次序 UNKNOWN |
| 8 光学定位 | clear中先做光学定位，耗时3秒 | 没有独立optical端点；不返回精确坐标 |
| 9 清除 | clear_result为success（总动作耗时5秒）或no_target_in_range（3秒）；20米内指定频道未清源必成功 | 不返回目标总数；每频道最多一源；near表示≤5米，可立即同位置clear |
| 10 结束 | 自行证明搜索与清除完成后，在任意位置exit；成功exit_reason=user_exit | 无all_done/count/status端点；达到10个清除不能推断完成，16个不同频道成功则达到数量上界 |
| 11 日志 | 客户端自行记录请求响应；正式结束后模拟器生成加密日志并入上传队列，界面可导出 | 日志扩展名、原名格式、存放位置 UNKNOWN；自动导出接口未文档化 |
| 12 正式限制 | 问题3共3次，启动成功即占机会，中止不返还；需联网；保留原名原内容；正式前冻结策略 | 每局25分钟窗口，enter后至多20分钟，取较早截止；虚拟限时100小时；每局日志上传≤2MB |

## 请求与错误处理

公共请求字段恰为 `arena_id="default"`、`robot_id=<当前登录参赛队号>`、`request_id=<本会话唯一ID>`。measure和clear额外包含 `position={"x":number,"y":number}`、`channel=1..20`。

必须串行动作，完整响应后才发送下一个新动作。网络故障重试必须保持路径、请求内容和request_id完全相同。服务器返回第一次完整响应，不重复计时。HTTP 409表示ID冲突或并发冲突。HTTP 400等协议错误不得当测量结果。

所有业务响应有accepted、real_timestamp_ms、virtual_time_s。仅HTTP 200且accepted为布尔true才更新状态。accepted=false中的virtual_time_s=0不是时钟归零。失败响应、超时、连接关闭分别记录。无法确定是否已执行的动作不得以新ID发送。

enter还返回max_virtual_duration_s、max_real_duration_s、remaining_real_duration_s。后者决定本局实际预算，不能固定假定1200秒。exit不能用于测试结束后的原因查询，接口届时已关闭。

## 时间模型

令合法动作之前的位置为p、当前测向频道为c，提交位置为q、频道为j：

`ΔT_measure = ||q-p||/5 + 1[j≠c] + 5`。

`ΔT_clear = ||q-p||/5 + 3 + 2·1[success]`，且c保持不变。

总时间为移动、测向切频、RF、光学、成功清除五项之和；enter/exit为0。失败光学也计入光学时间；拒绝请求不推进时间。内部微秒计时，响应最多6位小数，客户端核对保留微小量化容差，模拟器返回时间作为成绩权威。

## 物理与完成约束

问题3全部为全向源，10至16个，频道1至20互异；位于半径1800米圆内。有效接收半径各为1000至1500米。no_signal仅排除当前频道在测站1000米闭圆内存在未清除源，不证明整个频道为空。清除、定位与覆盖记录必须按频道维护。

严格完成条件可用：所有已发现源已清除，且对每个未发现/未清除频道，目标圆域均由该频道有效无信号测站的1000米圆覆盖。已清除频道无需再次覆盖。仅有限随机点检验不构成几何证明。

## 不可杜撰的能力

| 能力 | 审计结论 | 对用户计划的影响 |
| --- | --- | --- |
| 设置场景seed | UNKNOWN，无对应HTTP端点 | 无法按文档实施官方同seed配对 |
| reset/重开同一案例 | 已结束案例不能复位或续跑；未提供同案例重开机制 | 自动reset及真实环境同场景重放尚不可实现 |
| 自动开启新演练 | 界面支持开启，未提供HTTP控制 | 本环境原生桌面控制不可用，需手动开启或后续官方机制 |
| 演练后隐藏坐标 | 无任何获准导出机制；题面称数据无法直接读取 | 不解密、不检查数据库或进程内存；真实坐标oracle无法计算 |
| 演练后总数 | 界面显示，接口不返回 | 只能任务结束后补入评估文件，不给控制器 |
| 正式后总数/真值 | 界面和完成提示不显示 | 不得杜撰正式清除比例分母；报告成功数、完成证明及规定指标 |
| 模拟器源码与样例真实日志 | 可执行程序已到位；源码未提供；已取得一次真实连接演练日志 | 连接验证不等于完整搜索训练 |
| 问题1/2已有代码 | 新增shared.tex和shared_2.tex为算法文稿，已读取；没有可执行实现 | 复用算法定义，修正问题3所需的计时及误差处理 |

## 正式时间与原始文件

题面与附件规定北京时间2026-09-13 17:30后不可启动新测试，建议15:30前完成正式测试。已开始测试可结束，已入上传队列日志可继续上传。截止前enter应尽量在窗口开始5分钟内执行，以获得完整20分钟。正式原始日志仅从官方导出，保留原名和内容，客户端JSONL不能替代。
