# 第四问官方演练脚本

队伍号已填为 **202617201735**。算法固定为 **R2_S22_ML-validated-20260912**，执行源码 `09eaa82dd8fca503ba79b72b544b124d221483f5`，与上一轮通过100场本地验证的冻结版本相同。

## 怎么运行

1. 安装 Python 3.10 或更新版本。只用标准库，不需要额外安装 Python 库。
2. **完整解压 ZIP**，不要在压缩包内直接双击。双击 `start_q4_practice.cmd`。
3. 脚本先离线检查文件，然后停在确认提示。此时尚未连接官方模拟器。
4. 在官方模拟器中登录队号 **202617201735**，选择 **问题4 → 演练测试**，点击开始，等待5秒倒计时结束、接口开放。
5. 返回脚本窗口，输入 **Q4 PRACTICE** 并回车。脚本逐次检测、移动和清除，显示清除数量与虚拟时间。

官方公开接口不能返回当前题号或测试模式，因此必须在官方界面核对；这个脚本不能把一个已经打开的正式会话改成演练。脚本不自动登录、不启动官方测试、不切换模式。

## 队号和端口

如有变化，用记事本编辑 `practice_settings.json`，保留字符串引号。默认地址为 `http://127.0.0.1:2026`。队号必须与当前官方登录身份完全一致。端口只能在官方模拟器空闲时修改；本脚本仅支持本机数字回环地址。

不使用双击入口时，可在解压目录打开终端。只检查、不连接：

```text
python official_practice.py --check-only --robot-id 202617201735
```

在官方界面已选择问题4演练且接口已开放后运行：

```text
python official_practice.py --robot-id 202617201735 --confirm-practice I_HAVE_SELECTED_PRACTICE
```

修改端口时追加 `--url http://127.0.0.1:新端口`。

## 结果在哪里

`official_practice_logs/本次编号/` 中包含：

- `requests.jsonl`：实际请求、反馈、重试和决策记录。
- `summary.json`：完整性核验、实际清除数、总虚拟时间、每个已清除源的均时、现实耗时和分项时间。未证明完整成功时不输出每源成绩。
- `manifest.json`、`freeze.json`：运行版本、配置和哈希，便于追溯。

成功结果需同时通过反馈证书检查与独立退出核验，官方最终统计仍以模拟器界面为准。脚本只读取四个公开接口的反馈，不获得真实源数、位置、半径或朝向。

如果连接失败、动作被拒绝或运行被中断，请保存整个本次日志目录并查看官方界面。网络不确定时会以相同请求编号和原请求体有限重试；仍不确定即停止，不解释为无信号，不自动换新编号或退出。**不要在同一会话状态不明时直接重开脚本。**

## 本包自检范围

制作时只做离线校验与内存假接口测试，没有向官方模拟器发送请求。假接口用于检查入口、完整流程、丢失响应重试、拒绝响应和现实预算，不是官方成绩，也不新增优化验证场景。它可以通过下面的命令重复运行，且会阻止真实网络连接：

```text
python code/practice_offline_check.py --case lost_reply --output offline_checks
```

本包保留全部71个冻结源文件供哈希核验；其中本地实验工具不会被演练入口调用。不要删除或修改冻结目录中的文件，否则启动检查会拒绝运行。第三问 G 与原先的算法分享 ZIP 未更改。
