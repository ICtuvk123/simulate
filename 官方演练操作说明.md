# 自己运行官方问题3演练

本说明仅用于演练。当前网页是自建模拟器，不能通过网页上的“运行”按钮启动官方演练。

## 操作顺序

1. 双击项目目录中的 `start_official_practice.cmd`。它先核验代码，不会自动打开或开始官方测试。
2. 选择策略：回车选择当前 G；输入 F 可用上一版。G 已完成本地验证，F 已完成官方演练。输入官方当前登录的参赛队号；端口未改过就回车使用 2026。不要输入账号密码。
3. 打开 `Jammers-simulator-full-win64/Jammers-simulator-full/jammers-simulator-full.exe`，联网登录，选择 **问题3演练测试**。不要选择正式测试。
4. 等待数据准备和 5 秒倒计时结束，确认接口已开放。回到脚本，照官方页面输入测试标题“问题3演练测试”和当前案例编号；再次确认页面为演练，输入“演练”后回车，才会发送 `/enter` 并运行算法。
5. 等脚本自行搜索、清除并退出。界面中的行动时间按题目规则累加，不是电脑实际运行秒数。结束后核对官方显示的总干扰源数、已清除数和总时间；从官方日志列表导出本次演练原始日志，保留原文件名。

不要同时启动两个算法脚本。每次脚本只连接当前一个案例；下一场需要在官方软件中新建演练，再重新运行脚本。

## 保存位置和常见情况

- 请求、反馈、程序版本和计时记录在 `training_logs/<本次运行编号>/`；这些记录不能替代官方原始日志。
- “冻结文件改变”：当前算法文件与验证版本不一致，脚本会停止，先核对文件。
- “连接失败”：核对是否已结束倒计时、接口端口是否一致、官方软件是否仍在演练。若动作是否成功不明，不要直接重跑同一案例，先保留并检查日志。
- “案例已经运行过”或认领文件已存在：为避免重复进入，脚本停止；不要删除旧记录来强制重跑。
- 官方接口没有返回演练/正式模式字段。脚本依赖操作者读取当前官方页面，不能自动保证所选模式；“演练”确认不是来自官方接口的模式证明。
- 官方演练没有文档化的指定源数量入口，不能指定恰好 14 个。自建模拟器可以指定数量。
- 根据附件1，北京时间 **2026年9月13日17:30** 后不能开始新的演练。

如只想检查脚本文件，在终端运行 `start_official_practice.cmd --prepare-only`；此模式不会建立网络连接。

本入口于 2026-09-12 新增。交付时只进行离线预检和启动条件测试，没有替用户运行新的官方演练。
